% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04-sequence-translatePattern.R
\name{translatePattern}
\alias{translatePattern}
\title{Translate IUPAC Extended Genetic Alphabet patterns to regular expressions}
\usage{
translatePattern(pattern)
}
\arguments{
\item{pattern}{A single character string containing IUPAC nucleotide ambiguity
codes and/or standard nucleotides (A, C, G, T, U). The pattern is
automatically converted to uppercase. Must be a single character string
(not a vector).}
}
\value{
Returns a single character string containing the pattern represented
       as a regular expression. The returned string can be used with functions
       like \code{\link[base]{grep}}, \code{\link[base]{grepl}}, or
       \code{\link{countPatternInSeqs}} for pattern matching in DNA/RNA
       sequences.
}
\description{
Converts DNA/RNA patterns containing IUPAC nucleotide ambiguity codes to regular
expressions suitable for pattern matching in DNA/RNA sequences. This function
is useful for searching sequences with degenerate motifs or consensus sequences
that contain ambiguous nucleotides.

The function automatically converts the input pattern to uppercase and
translates each IUPAC ambiguity code to its corresponding regular expression
character class. Standard nucleotides (A, C, G, T, U) are left unchanged.

\strong{IUPAC nucleotide ambiguity codes and their translations:}
\itemize{
  \item \code{Y} -> \code{[C|T]} (pyrimidine: C or T)
  \item \code{R} -> \code{[A|G]} (purine: A or G)
  \item \code{S} -> \code{[G|C]} (strong: G or C)
  \item \code{W} -> \code{[A|T]} (weak: A or T)
  \item \code{K} -> \code{[T|U|G]} (keto: T, U, or G)
  \item \code{M} -> \code{[A|C]} (amino: A or C)
  \item \code{B} -> \code{[C|G|T]} (not A: C, G, or T)
  \item \code{D} -> \code{[A|G|T]} (not C: A, G, or T)
  \item \code{H} -> \code{[A|C|T]} (not G: A, C, or T)
  \item \code{V} -> \code{[A|C|G]} (not T: A, C, or G)
  \item \code{N} -> \code{[A|C|T|G]} (any nucleotide: A, C, T, or G)
}

Standard nucleotides (\code{A}, \code{C}, \code{G}, \code{T}, \code{U}) are
not modified and remain as literal characters in the regular expression.
}
\details{
The function performs the following steps:
\enumerate{
  \item Validates that \code{pattern} is a single character string
  \item Converts the pattern to uppercase using \code{\link[base]{toupper}}
  \item Replaces each IUPAC ambiguity code with its corresponding regular
        expression character class using fixed string matching
  \item Returns the translated pattern
}

The regular expressions use character classes with the pipe operator (\code{|})
to represent alternative nucleotides. For example, \code{"[A|G]"} matches
either A or G.
}
\examples{
## Example 1: Simple pattern with ambiguity codes
pattern1 <- "AACCNWMK"
translatePattern(pattern1)
## Returns: "AACC[A|T][A|C][T|U|G][A|C]"

## Example 2: Pattern with N (any nucleotide)
pattern2 <- "ATGNAT"
translatePattern(pattern2)
## Returns: "ATG[A|C|T|G]AT"

## Example 3: Pattern with multiple ambiguity codes
pattern3 <- "YYRRSSWW"
translatePattern(pattern3)
## Returns: "[C|T][C|T][A|G][A|G][G|C][G|C][A|T][A|T]"

## Example 4: Pattern is automatically converted to uppercase
pattern4 <- "aaccnwm"
translatePattern(pattern4)
## Returns: "AACC[A|T][A|C][T|U|G][A|C]"

}
\seealso{
\code{\link{countPatternInSeqs}} for counting pattern occurrences
         in sequences, \code{\link{summarizePatternInPeaks}} for summarizing
         pattern occurrences in peak regions
}
\author{
Lihua Julie Zhu
}
\keyword{misc}
