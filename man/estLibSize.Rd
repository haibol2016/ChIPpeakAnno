% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-utilities-estLibSize.R
\name{estLibSize}
\alias{estLibSize}
\title{Estimate library size from BAM files}
\usage{
estLibSize(bamfiles, index = bamfiles, ...)
}
\arguments{
\item{bamfiles}{A character vector of file paths to BAM files to be
processed. Multiple files can be processed in a single call. The BAM
files should be indexed (have corresponding .bai files) for optimal
performance, though the function can work without indexes.}

\item{index}{A character vector of index file names (without '.bai'
extension) corresponding to \code{bamfiles}. Defaults to \code{bamfiles},
meaning the index files are assumed to have the same base name as the
BAM files with a '.bai' extension. If your index files have different
names, specify them explicitly here. Can also be an empty character
vector (length 0) or a list of empty character vectors to indicate no
index files.}

\item{...}{Additional arguments (currently not used).}
}
\value{
Returns a numeric vector of library sizes, one value per BAM file
       in \code{bamfiles}. The values represent:
       \itemize{
         \item \strong{Paired-end data}: Number of read pairs (proper pairs
               divided by 2)
         \item \strong{Single-end data}: Number of reads (mapped, non-secondary
               reads)
       }
       The return value is named with the BAM file paths if multiple files
       are provided.
}
\description{
Estimates the library size (total number of reads or read pairs) from BAM
files. The function automatically detects whether the data is paired-end or
single-end and counts accordingly. Library size is a key metric for
normalization and quality control in sequencing experiments.

The function uses efficient counting methods that don't require loading reads
into memory, making it fast even for large BAM files.
}
\details{
\strong{How the function works:}
\enumerate{
  \item For each BAM file, tests if it contains paired-end reads using
        \code{\link[Rsamtools]{testPairedEndBam}}
  \item Based on the data type, counts reads using
        \code{\link[Rsamtools]{countBam}} with appropriate filters
  \item Returns the count (adjusted for paired-end data)
}

\strong{Paired-end mode:}
For paired-end data, the function:
\itemize{
  \item Counts only proper pairs (both mates mapped, correct orientation,
        same chromosome)
  \item Excludes secondary alignments and low-quality reads
  \item Divides the count by 2 to get the number of read pairs (since each
        pair contributes 2 records to the BAM file)
  \item Returns the number of read pairs as the library size
}

\strong{Single-end mode:}
For single-end data, the function:
\itemize{
  \item Counts all mapped reads (excluding unmapped reads)
  \item Excludes secondary alignments and low-quality reads
  \item Returns the total number of reads as the library size
}

\strong{Read filtering:}
The function applies strict filtering to ensure only high-quality reads are
counted:
\itemize{
  \item \strong{Paired-end}: Proper pairs only, no secondary alignments,
        passing quality controls
  \item \strong{Single-end}: Mapped reads only, no secondary alignments,
        passing quality controls
}
Secondary alignments are excluded because they represent alternative
alignments for reads that have multiple mapping locations, and counting them
would inflate the library size.

\strong{Performance:}
The function uses \code{countBam}, which is highly efficient because it:
\itemize{
  \item Counts reads directly from the BAM index without loading reads into
        memory
  \item Processes files quickly even for very large libraries
  \item Can handle multiple files efficiently
}

\strong{Use cases:}
Library size estimation is important for:
\itemize{
  \item Normalization in differential expression analysis (e.g., DESeq2,
        edgeR)
  \item Quality control (comparing library sizes across samples)
  \item Downstream analysis (e.g., calculating reads per million, RPM)
  \item Reporting in publications and data summaries
}

\strong{Index files:}
While the function can work without index files, having them (.bai files)
significantly improves performance. If index files are not found, the
function will still work but may be slower for large files.
}
\note{
\itemize{
  \item The function automatically detects paired-end vs single-end data
  \item For paired-end data, the result is the number of read pairs, not the
        total number of reads (which would be 2x the library size)
  \item Secondary alignments are excluded to avoid double-counting
  \item Low-quality reads (failing quality controls) are excluded
  \item The function uses efficient counting methods and is fast even for
        large BAM files
  \item Index files improve performance but are not strictly required
}
}
\examples{

# Example 1: Estimate library size for a single BAM file
\dontrun{
bamfile <- "sample.bam"
lib_size <- estLibSize(bamfile)
lib_size
}

# Example 2: Estimate library size for multiple BAM files
\dontrun{
bamfiles <- c("sample1.bam", "sample2.bam", "sample3.bam")
lib_sizes <- estLibSize(bamfiles)
lib_sizes

# Compare library sizes across samples
barplot(lib_sizes, names.arg = basename(bamfiles),
        ylab = "Library Size", main = "Library Sizes")
}

# Example 3: Specify custom index file names
\dontrun{
bamfile <- "sample.bam"
index_file <- "custom_index"  # without .bai extension
lib_size <- estLibSize(bamfile, index = index_file)
}

# Example 4: Calculate reads per million (RPM) normalization factor
\dontrun{
lib_size <- estLibSize("sample.bam")
rpm_factor <- 1e6 / lib_size
# Use rpm_factor to normalize read counts
}

# Example 5: Using with example data (if available)
if (interactive() || Sys.getenv("USER") == "jou") {
    path <- system.file("extdata", "reads", package = "MMDiffBamSubset")
    if (file.exists(path)) {
        WT.AB2 <- file.path(path, "WT_2.bam")
        Null.AB2 <- file.path(path, "Null_2.bam")
        Resc.AB2 <- file.path(path, "Resc_2.bam")
        estLibSize(c(WT.AB2, Null.AB2, Resc.AB2))
    }
}
}
\seealso{
\itemize{
  \item \code{\link[Rsamtools]{testPairedEndBam}} for detecting paired-end
        data
  \item \code{\link[Rsamtools]{countBam}} for efficient read counting
  \item \code{\link[Rsamtools]{ScanBamParam}} for read filtering parameters
  \item \code{\link{estFragmentLength}} for estimating fragment lengths
}
}
\author{
Jianhong Ou
}
\keyword{misc}
